all
{
        %CC sm src/*.c
}

clean
{
        !rm -f sm
}

install root (all)
{
        !cp -f sm /usr/local/bin/sm
}

debug
{
        %SET CFLAGS -Og -Wall -pedantic -Wextra -fsanitize=address
        %CC sm src/*.c
}
